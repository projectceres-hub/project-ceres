# Git Workflow for Project Ceres

- Use `./git-save.sh "message"` (or `./git-save.ps1`) to:
  - add all changes
  - commit with a message
  - push to GitHub

Recommended:
- One feature or bug fix per commit where possible.
- Run tests before pushing when automation jobs or schedulers are touched.
